<aside>
  <div>
    <a href="kith/new">+ Add Person</a>
  </div>
  <h3>Kith & Kin List</h3>
  <ul>
    <% persons.forEach(function(p) { %>
    <li>
      <a href="/kith/<%= p._id %>"><%= `${p.firstName} ${p.lastName}` %></a>
    </li>
    <% }) %>
  </ul>
</aside>
